# 肌电手环遥操灵巧手项目交接文档

更新时间：2026-09-20  
代码目录：`D:\桌面\肌电手环_源码版\肌电手环_源码版`

## 1. 当前结论

离线工程链路已经打通：历史数据审计 → 统一转换 → 工程基线模型 → 流式推理 → 上位机离线回放 → 正式采集计划。

当前**还没有可用于证明真实三分类精度的训练数据和模型**。现有历史数据只能用于诊断、格式验证和离线回放，不能用于正式训练，也不能伪装为 `canonical_session`。

接手后的第一优先级不是继续调模型，而是拿到手环后确认真实采样率、BLE 质量和佩戴质量，再按正式协议采集 `rest / fist / open_hand` 数据。

## 2. 六项任务完成状态

| 项目 | 状态 | 结果 |
|---|---|---|
| 1. 审核历史数据 | 已完成 | 7 个会话、153,949 行；没有任何正式训练候选会话 |
| 2. 历史数据转换 | 已完成 | 生成 v4 诊断数据；旧 v3 与新 v4 均可由官方 loader 读取 |
| 3. 三分类基础模型 | 工程验证完成 | `rest / fist / open_hand` 合成数据模型已跑通；不能代表真实准确率 |
| 4. 流式推理 | 已完成 | 支持置信度门限、连续窗口防抖、unknown/hold、断连复位 |
| 5. 上位机离线回放 | 已完成 | 已接入训练采集 GUI，支持进度、取消、停止报告和异常清理 |
| 6. 正式采集数量 | 已制定初版 | 10 人，每人 3 次独立佩戴，每次每动作 10 个 5 秒录制，共 90 个录制/人 |

## 3. 历史数据审核结果

数据来源：

- 原历史数据：`D:\桌面\肌电手环\肌电手环\patient_data`
- 旧转换数据：`D:\桌面\肌电手环\肌电手环\patient_data\legacy_experimental_20260906_v3`
- 新转换数据：`artifacts/history_conversion/legacy_experimental_v4`
- 审核报告：`artifacts/history_audit/legacy_audit_v3.json`

审核结果：

- 7 个历史会话，共 153,949 行上位机写入记录。
- 可用于诊断的会话：S1、S2、S3、S4、S6。
- 正式训练候选：0。
- 只有 1 名受试者。
- 没有独立、持续的 `open_hand` 标签。
- 真实采样率、设备时间戳和包身份未确认。
- 历史标签包含 `rest`、`stable_fist`、`slow_open_close`、`fast_open_close`、`transition`、`unknown`、`wrist_motion_artifact`，不等同于正式三分类协议。

因此，历史数据统一标记为：

```text
training_provenance = legacy_experimental
training_usable = false
```

## 4. 模型与流式推理现状

核心文件：

- `training_contract.py`：正式训练合同，固定 8 通道。
- `training_dataset.py`：正式 `canonical_session` 准入和数据加载。
- `emg_features.py`：MAV、RMS、WL、ZC、SSC 特征。
- `train_emg_baseline.py`：StandardScaler + shrinkage LDA 三分类训练。
- `emg_model_bundle.py`：严格、版本化模型包。
- `realtime_emg_inference.py`：200 ms 窗口、50 ms 步长的流式推理和决策防抖。
- `benchmark_emg_inference.py`：仅测特征和模型计算耗时。

工程基线产物：`artifacts/model_baseline/`

当前合成测试结果：

- 三类测试集准确率：100%。
- 混淆矩阵：每类 97 个窗口全部正确。
- 推理计算总耗时 p99：约 0.116 ms。
- 模型包：`emg-lda-0babc8a27f716bb81e1fc387`。

这些结果只证明代码和接口能工作。报告已明确标记：

```text
provenance = synthetic_test
deployment_status = engineering_only
live_control_eligibility = not_evaluated
deployment_gate_passed = false
```

禁止把 100% 合成准确率写成真实手环准确率，也禁止用该工程模型直接控制灵巧手。

## 5. 上位机离线回放现状

核心文件：

- `shared_memory_v2.py`：共享文件协议及真实控制 fail-closed 门禁。
- `replay_emg_session.py`：严格历史数据校验、模拟时间轴和流式推理。
- `qt5_training_capture.py`：训练采集程序及独立离线回放状态。

回放产物：

- `artifacts/leader_validation/s2_v4_engineering_replay_report.json`
- `artifacts/leader_validation/s2_v4_engineering_replay.bin`

S2 实跑结果：

- 输入 2,492 行，写入 2,492 个数据帧。
- 产生 246 次流式决策：`open_hand=244`、`unknown=2`。
- 最终 sequence 为 2,493，并写入断连终止帧。
- 状态为 completed，errors 为空。
- 回放帧带 `REPLAY`、`SYNTHETIC_TIME` 标志，真实控制读取接口会拒绝。
- 回放使用假设的 200 Hz 合成时间轴，不是设备真实采样率证据。

注意：S2 的预测分布不能用于评价模型准确率。它反而说明合成模型与真实历史信号存在明显域差异。

已实现的安全边界：

- 真实控制只接受 8 通道、正确连接代次的当前实时帧。
- 拒绝 replay、synthetic time、stale、disconnected、overflow、CRC error。
- 回放前后复核 CSV、manifest、原始数据和标注文件的 size/hash，运行中被修改或删除会失败。
- GUI 关闭或重复取消时，会等待后台线程真正退出，防止退出后继续写共享文件。

## 6. 正式采集计划

计划文件：

- `artifacts/collection_plan/collection_plan.md`
- `artifacts/collection_plan/collection_plan.json`

初始数量：

- 10 名受试者。
- 每人 3 次独立佩戴，最好安排在不同日期。
- 每次佩戴采 `rest`、`fist`、`open_hand`。
- 每个动作采 10 个独立 recording/trial。
- 每个 trial 保持 5 秒。
- 每人共 3 × 3 × 10 = 90 个录制。
- 两个 trial 间休息 10 秒；动作组之间休息 60 秒。

一次训练采集 GUI 录制等于一个固定动作 trial。建议 ID：

```text
experiment_batch = exp-YYYYMMDD-protocol-v1
visit_id          = visit-01 / visit-02 / visit-03
session_id        = {subject_id}_{visit_id}_{action_label}_trial-{trial_number:02d}
```

失败或不合格的 trial 不覆盖、不改标签，保留拒绝证据并使用 `_retry-01` 重新录制。

数量仍是初版。拿到真实数据后，在每动作 5/10/20/30 个录制以及 3/5/8/10 名受试者处画学习曲线；只有连续两个检查点 macro-F1 提升小于 0.02，且每类 recall ≥ 0.85，才冻结最终数量。

## 7. 拿到手环后的执行顺序

1. 确认设备真实采样率、ADC/缩放含义和 16 字节 BLE 包字段。
2. 实测 BLE 丢包、重复、乱序、过期值和有效采样率。
3. 检查电极佩戴、脱落和重复佩戴对信号的影响。
4. 用训练采集程序先采 3 人小规模 pilot，不要直接采满 10 人。
5. 确认所有会话均为 `canonical_session` 且 `session_quality.training_usable=true`。
6. 用受试者隔离的 train/validation/test 划分训练真实三分类模型。
7. 查看混淆矩阵、每类 recall、错误窗口和推理耗时，更新采集数量。
8. 真实模型通过后，再接灵巧手控制层并测试 BLE→推理→执行器端到端延迟。

## 8. 暂时无法完成的硬件项

- 确认真实采样率。
- 测量真实 BLE 丢包率、重复率和乱序率。
- 验证电极佩戴效果及重复佩戴稳定性。
- 完成新训练采集程序的真实硬件验收。
- 测量 BLE→推理→灵巧手的端到端控制延迟。

这些项目未完成前，`deployment_gate_passed` 必须保持 false。

## 9. 验证与审查状态

最终验证：

```text
Pyright：14 个核心文件，0 errors，0 warnings
pytest：794 passed，7 skipped，255 subtests passed
py_compile：14 个核心文件通过
```

5 条 warning 均为既有 Bleak deprecated API 提示，不是本轮失败。

严格审查结果：

- code-reviewer：APPROVE
- architect：CLEAR
- 共进行了 3 轮返工：
  1. 修复历史版本兼容、统一严格验证、GUI 回放、采集术语和合成报告语义。
  2. 修复真实控制 fail-closed、回放 TOCTOU 和 GUI 双取消/后台线程清理。
  3. 修复静态类型问题，使 14 个核心文件 Pyright 零错误。

全量测试命令：

```powershell
python -m pytest -q
```

静态检查命令：

```powershell
uvx pyright audit_legacy_dataset.py convert_legacy_dataset.py legacy_dataset.py training_contract.py training_dataset.py train_emg_baseline.py emg_model_bundle.py benchmark_emg_inference.py engineering_baseline_demo.py collection_protocol.py realtime_emg_inference.py shared_memory_v2.py replay_emg_session.py qt5_training_capture.py --pythonpath "C:\Users\26863\AppData\Local\Programs\Python\Python310\python.exe" --pythonversion 3.10 --level error
```

## 10. 剩余低风险事项

- GUI 离线回放目前按左手模型打开；未来支持右手时，应从模型包和会话合同推导佩戴侧。
- `replay_code_version` 目前是人工版本号，不是源码 digest；法规级审计时需要加入构建/source hash。
- Bleak 的 `set_notification_handler()` 已弃用，后续可迁移到 `add_notification_listener()`。

## 11. 最重要的交接原则

1. 历史数据永远保持 `legacy_experimental`，不能升级或改写成 `canonical_session`。
2. 合成模型只能用于工程验证，不能报告为真实准确率。
3. replay/synthetic 数据绝不能驱动真实灵巧手。
4. 正式模型只接收通过质量门禁的 8 通道 canonical 数据。
5. 优先增加受试者和独立佩戴次数，不要靠切出更多高度相关窗口虚增样本量。

## 12. 单受试者个性化灵巧手采集计划（v2）

用户提出的方向正确，但“至少 20/10/10 分钟”是下限，不是上限。为了让 train、validation 和 sealed-test 都有独立且完整的因子覆盖，正式配额提高到 **160.4 分钟、764 个 trials**；另采 4 分钟/24 个 transition trials，全部合计 **164.4 分钟、788 个 trials**。

- 结构化连续屈伸：每个 split 都完整覆盖 11 指组 × 3 速度 × 4 ROM = 132 条件；三个 split 共 396 trials、79.2 分钟。
- 激活 hold 正样本：每个 split 都完整覆盖 11 指组 × 4 ROM = 44 条件；共 132 trials、17.6 分钟。
- rest/no-action：4 分钟；自由连续动作：10 分钟。
- 闭环困难样本：第一版模型冻结后采 10 分钟，使用新的 `donning-04/train-hard` 和 `donning-05/validation-hard` 各 10 个 trial；sealed-test 为 0，不能复用已结束的 01/02。
- 鲁棒性：每个 split 独立佩戴，并覆盖 3 前臂姿态 × rested/fatigued × 11 指组，共 39.6 分钟。

三个 base split 使用不同日期、session 和 donning：train=`donning-01`，validation=`donning-02`，sealed-test=`donning-03`；加上独立 hard-mining 的 04/05，本协议最少需要 **5 次独立佩戴**。Transition 按 8/8/8 分配给三个 base split，作为 phase 分类正类和 phase 指标，但不计最低时长。必须先分组再切窗口；测试数据及其错误、预测和元数据永远不能用于困难样本挖掘、调参或重训练。单人结果不能证明跨人泛化。

速度提示为 slow=0.25 Hz、normal=0.5 Hz、fast=1.0 Hz；ROM 提示为 20%/50%/80%/100%。`commanded_phase/ROM/speed` 只代表提示，`measured_phase/ROM/speed` 只能来自同步、校准充分的摄像头、数据手套或编码器。

拟合前门禁只读 train/validation，禁止提前访问 sealed-test 标签和真值。公共门限为同步误差 p95≤20 ms、最大≤50 ms，并在每个 session/重新佩戴后校准。ROM 头要求校准误差≤5 个百分点、每 trial 覆盖≥95%、每个 train/validation×finger×ROM×phase 单元≥90%。速度头独立要求校准绝对误差≤0.05 Hz、相对误差≤10%、每 trial 覆盖≥95%、每个 train/validation×finger×speed×phase 单元≥90%；失败则该模型版本永久禁用速度回归和对应指标。

测试封存前只允许文件、通道、包、时间戳、丢包和设备质量等模型无关 QC。最终模型与分析代码冻结后只解封一次，独立验证 test truth；失败则相应测试无效，且禁止把失败、标签、真值、预测或错误反馈给采集、调参或重训练。

疲劳诱导最多 60 次或 300 秒，连续主动工作最多 120 秒，每 10 次或 60 秒复查 RPE，之后强制恢复 300 秒。疼痛、麻木、抽筋、眩晕、异常无力、RPE≥8、受试者要求或触及硬限制时立即停止；恢复后 RPE 仍大于 2 或症状未消失则当天终止。该规则只用于工程安全管理，不构成医疗建议。

阶段 0 继续绑定现有 `collection_protocol.py` 的 schema/version/SHA-256、`canonical_session` provenance 和 `session_quality.training_usable=true` 门禁，只做 `rest/fist/open_hand` 粗分类链路验证。精细控制在外部真值门禁通过后采用动作/手指集合分类 + ROM 连续回归，必要时增加速度回归。

执行文件：

- `personalized_collection_protocol.py`
- `artifacts/personalized_collection_plan/personalized_collection_plan.json`
- `artifacts/personalized_collection_plan/personalized_collection_plan.md`

拿到手环后的下一步：先确认真实采样率、BLE 质量和佩戴稳定性；再接入外部真值，按 5 次独立佩戴时序完成小规模 pilot 并验证门禁；通过后才扩大到完整 164.4 分钟计划。
