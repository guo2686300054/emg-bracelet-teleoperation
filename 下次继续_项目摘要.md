# 下次继续：肌电手环遥操灵巧手项目摘要

更新时间：2026-09-20

## 项目位置

- 本地源码：`D:\桌面\肌电手环_源码版\肌电手环_源码版`
- GitHub 私有仓库：`https://github.com/guo2686300054/emg-bracelet-teleoperation`
- 当前分支：`main`
- 首次推送提交：`0f8745df62a3dcd19aeb794d02930f7da50cc7a6`
- 完整交接文档：`项目交接文档.md`

## 最终目标

使用 8 通道 sEMG 肌电手环实时遥操灵巧手。当前采集与离线工程链路已经实现，下一阶段是确认硬件参数、正式采集个人数据、训练真实模型，然后接入灵巧手控制。

## 已完成

1. 升级共享文件协议，包含 magic、version、timestamp、sequence、valid_length、flags 和连接代次。
2. CSV/会话记录增加时间、样本、包、动作、采样率、设备和 provenance 等字段。
3. 修复 `test.py` 路径定位、中文显示和初始化失败后的安全关闭。
4. 增加搜索、连接、notify、保存、共享文件和异常日志。
5. 实现历史数据审计与严格转换；legacy 永远不能伪装为 `canonical_session`。
6. 实现 8 通道三分类工程基线、模型包、错误窗口、混淆矩阵和推理延迟测试。
7. 实现流式推理、置信度门限、平滑、防抖、unknown/hold 和断连复位。
8. 训练采集上位机支持独立离线回放、进度、取消、停止报告和异常清理。
9. 回放帧带 REPLAY/SYNTHETIC_TIME 标志，真实控制入口 fail-closed 拒绝。
10. 完成单受试者个性化精细动作采集协议。

## 历史数据结论

- 7 个历史会话，共 153,949 行上位机写入记录。
- 只有一个人，没有独立持续的 `open_hand` 标签。
- 真实采样率、设备时间戳和包身份未确认。
- 正式训练候选会话：0。
- 历史数据只能用于诊断、格式验证和离线回放。
- 不能用历史数据声明真实三分类准确率。

## 当前模型结论

- 当前 `rest / fist / open_hand` 模型使用合成数据，只是阶段 0 工程验证。
- 合成测试准确率 100% 不代表真实手环准确率。
- `deployment_status=engineering_only`。
- `deployment_gate_passed=false`。
- 禁止直接驱动真实灵巧手。

## 单受试者正式采集协议

执行文件：

- `artifacts/personalized_collection_plan/personalized_collection_plan.md`
- `artifacts/personalized_collection_plan/personalized_collection_plan.json`
- `personalized_collection_protocol.py`

数量：

- 配额数据：764 trials，160.4 分钟有效信号。
- Transition：24 trials，4 分钟，不计最低配额。
- 总计：788 trials，164.4 分钟。
- 倒计时、休息、校准、无效帧和重采不计入上述时长。

覆盖内容：

- 单指：拇指、食指、中指、无名指、小指。
- 组合：食+中、环+小、拇+食、拇+中、拇食中、五指。
- 速度：0.25 / 0.5 / 1 Hz。
- ROM：20% / 50% / 80% / 100%。
- Phase：flexion、extension、hold、transition、rest/no-action。
- 自由连续动作至少 10 分钟。
- 初版模型后的闭环困难样本至少 10 分钟。
- 中立、旋前、旋后；疲劳前后；至少 5 次独立重戴。

数据隔离：

- `donning-01`：train 基础数据。
- `donning-02`：validation 基础数据。
- `donning-03`：sealed-test 基础数据。
- `donning-04`：初模冻结后的 train hard-mining。
- `donning-05`：初模冻结后的 validation hard-mining。
- sealed-test 最终只解封一次，闭环困难样本不得进入测试集。

真值边界：

- 没有同步摄像头、数据手套或编码器时，只允许分类训练。
- commanded ROM/speed 不能冒充 measured ROM/speed。
- ROM/速度回归只有通过独立真值、同步、校准和覆盖率门禁后才能启用。

## 暂时无法完成的硬件项

1. 确认真实采样率和 ADC/缩放含义。
2. 测量 BLE 丢包、重复、乱序和过期值。
3. 验证电极佩戴和重新佩戴稳定性。
4. 完成训练采集程序的真实硬件验收。
5. 测量 BLE→推理→灵巧手端到端延迟。

## 拿到手环后的下一步

1. 先做设备参数和 BLE 质量确认，不立即采满全部数据。
2. 使用阶段 0 采少量 `rest / fist / open_hand` canonical 数据，验证真实模型链路。
3. 通过后按个性化协议分多天完成至少 5 次独立佩戴。
4. 使用 date/session/donning 隔离训练、验证和测试，禁止随机切窗泄漏。
5. 先训练动作/手指集合分类；有可靠外部真值后再增加 ROM/速度回归。
6. 最后接入灵巧手控制并进行端到端延迟与安全测试。

## 当前质量状态

- 主项目全量测试：794 passed，7 skipped，255 subtests。
- 14 个核心文件 Pyright：0 errors，0 warnings。
- 个性化采集协议联合测试：46 passed。
- code-reviewer：APPROVE。
- architect：CLEAR。

## 下次会话直接发送

```text
请读取 D:\桌面\肌电手环_源码版\肌电手环_源码版\下次继续_项目摘要.md 和 项目交接文档.md，检查 GitHub main 分支状态，然后从“拿到手环后的下一步”继续。不要把 legacy 或 synthetic 数据当成正式训练数据。
```
